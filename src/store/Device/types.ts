export type TypeTypes = {
    id: number,
    name: string,
};

export type TypeBrands = {
    id: number,
    name: string,
};

export type TypeDevice = {
    id: number,
    name: string,
    price: number,
    rating: number,
    img: string,
};